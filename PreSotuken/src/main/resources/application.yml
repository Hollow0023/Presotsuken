spring:
  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://localhost:3306/order
    username: root
    password: pass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

server:
  address: 0.0.0.0
  port: 8080
